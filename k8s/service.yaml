apiVersion: v1
kind: Service
metadata:
  name: task-manager-service
spec:
  selector:
    app: task-manager
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30002
  type: NodePort
